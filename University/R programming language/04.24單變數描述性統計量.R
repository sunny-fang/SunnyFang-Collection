library(car)
library(ggplot2)# ?????????????????????
data(Salaries)# ????????????
View(Salaries)# ?????????,??????:V?????????
# ?????????????????????????????????
# rank: Qualitive, Discrete, Ordinal: Factor(??????????????????Factor)
# displine: Qualitative, Decrete, nominal: Factor
# salary: Quntitative, Continuous, ration: numeric(???????????????int,?????????)

# ????????????,???$??????,python??????.??????
# ????????????????????????????????????
mean(Salaries$salary)# ??????????????????(?????????????????????)
median(Salaries$salary)# ?????????????????????
quantile(Salaries$salary)# ????????????
quantile(Salaries$salary,c(0.1,0.2,0.8))# ?????????????????????

library(psych)# ?????????????????????????????????
dat <- c(1.01,1.015,1.025,1.03,1.03)
geometric.mean(dat)
?unique
Salaries$rank# ??????Salaries????????????rank
Salaries$discipline
uniqv <- unique(Salaries$rank)# ????????????????????????
t1 <- table(Salaries$rank)# ?????????????????????????????????,??????t1???run
max(t1)
w1 <- which.max(t1)# ???????????????????????????,??????????????????
t1[w1]# ?????????????????????,??????????????????(??????t1??????w1)

# ???????????????,??????????????????
unique(Salaries$rank)# ?????????????????????rank
getmode <- function(v){
  uniqv <- unique(v)# ?????????????????????
  uniqv[which.max(tabulate(match(v,uniqv)))]# ?????????????????????????????????
  
}
getmode(Salaries$rank)# ?????????professor

t1 <- table(Salaries$sex)# ????????????
t1a <- addmargins(t1)# ?????????????????????????????????
t1p <- prop.table(t1)# ???????????????????????????,??????prop,???????????????
t1pr <- addmargins(t1p)# ???????????????,????????????
round(t1pr,2)# ????????????????????????

# ????????????????????????
range(Salaries$yrs.service)
var(Salaries$yrs.service)
sd(Salaries$yrs.service)
# ??????????????????????????????,??????????????????????????????????????????
